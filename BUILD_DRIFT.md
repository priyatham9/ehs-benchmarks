# Build Drift Report

## Summary

The docs/ folder was patched directly on 2026-09-06 without rebuilding the TypeScript source. This report documents the changes needed to restore consistency between source and output.

## Environment

- Node.js is not installed on the analysis system
- No TypeScript compilation was performed to validate whether source templates exist
- All changes are direct edits to docs/index.html

## Drifted Files

### docs/index.html

Commit: `ea9c53556d81303a9297ca4657d5fdd4f4c4692b`
Message: "Join the Grounded programme navigation; allow indexing; canonical URLs"
Date: 2026-09-06 17:01:29 -0400

#### Change 1: Title attribute (em-dash to hyphen)
- **Line 6**: `<title>` changed from `EHS Benchmarks — open...` to `EHS Benchmarks - open...`
- **Category**: Metadata, searchable text
- **Scope**: User-facing title in browser tab

#### Change 2: Canonical link added
- **After line 11**: Added `<link rel="canonical" href="https://priyatham9.github.io/ehs-benchmarks/" />`
- **Category**: SEO, crawlability
- **Purpose**: Explicit canonical URL for search engines

#### Change 3: Programme navigation bar added
- **After line 12**: Added sticky navigation at top of page with inline styles
  ```html
  <nav class="pgm" aria-label="Programme">
  <style>.pgm{...}</style>
  ...links to Grounded hub and other projects...
  </nav>
  ```
- **Category**: Navigation, component addition
- **Styling**: Inline CSS with dark mode support via `@media(prefers-color-scheme:dark)`
- **Contains**: 7 navigation links to other projects in the Grounded portfolio

#### Change 4: Em-dashes replaced with hyphens (11 occurrences)
- **Lines affected**: 43, 71, 72, 81, 84, 87, 97, 136, 161, 188, 221
- **Pattern**: All instances of em-dash `—` replaced with hyphen `-` in prose content
- **Locations**:
  - Line 43: "data — 1.18" (tagline)
  - Line 71: "any good?" — and"
  - Line 81: "Finding 1 — the 200,000-hour trap"
  - Line 84: "Finding 2 — zero is not excellence"
  - Line 87: "Finding 3 — injuries cluster inside the shift"
  - Line 97: "CY2025 — not against"
  - Line 136: "Search by name — results"
  - Line 161: "System — event, body part"
  - Line 164: "Event — what happened"
  - Line 188: "inside it — sector"
  - Line 221: "licensed — " (with trailing space)

#### Change 5: Previous/Next navigation footer added
- **After line 231**: Added nav grid with links to Calculator (prev) and Grounded (next)
  ```html
  <nav aria-label="Programme navigation" style="display:grid;...">
  <a href="...Calculator...">Previous...</a>
  <a href="...Grounded...">Next...</a>
  </nav>
  ```
- **Category**: Navigation, component addition
- **Styling**: Inline CSS grid layout

## Porting Strategy

Since docs/ contains hand-coded HTML (no generated templates in source), these changes must be ported into:
1. **docs/index.html template source** (if a source template exists in src/)
2. **Build pipeline** (if index.html is generated by a build script)
3. **Direct source edit** (if index.html is the canonical source file)

Current status: docs/index.html appears to be the canonical source. No TypeScript template or build pipeline was found that generates it.

## Residual Notes

- No .github/workflows/ directory exists
- No build step in package.json generates docs/index.html
- COMMON.md rules specify no em-dashes in any files; these changes enforce that constraint
- The programme navigation bar follows the design pattern in repos/grounded/docs/index.html

## Next Steps

1. Install Node.js to run the TypeScript build
2. Verify whether any of these files are generated from source templates
3. If index.html is indeed canonical, a fresh build (tsc + npm run build) should reproduce docs/ exactly
4. If drift persists, investigate whether the build pipeline needs configuration updates
